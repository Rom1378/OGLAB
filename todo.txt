Improve textures binding for model renderer
Add more lights to shaders computation
Improve light manager
Add loading model instead of showing white window
Reduce memory leak
Implement scene save, load, saving all object states
Add batch rendering for cubes
Add mutli threading for loading models
Add spinning cat music for sound : )
X releasing objects, not working


X need a way to select individual objects.
	via UI and next via clicking from the camera with shift or smthg

X there is a delay on the input in the window for cusor sure keys idk.


fixing res folder. there is a copi when running cmake. choose what I want to do.

height map generation

add water at level -378

Add cool fps camera